gitHubAccessToken = "" # The GitHub access token to retrieve config files from your service repositories if they are private.
repoOrgOrUser = "" # The org or user that the repositories for your services sit within. E.g. "peter-hazell".
serviceConfigFile = "" # The path in the service repo to the config file that describes the service interactions. E.g. "master/conf/application.conf".